# CURSOR PROMPT TEMPLATE

## 🧠 BASE PROMPT

Always start with:

"Read AI_RULES.md and SYSTEM_CONTEXT.md and follow them strictly."

---

## 🔧 MODULE CREATION

Example:

"Read AI_RULES.md and SYSTEM_CONTEXT.md.

Create booking API behavior using Next.js Route Handlers + Prisma (services under `src/server`).

Requirements:
- Follow modular architecture
- Use service + schema separation (add or extend `api-router.ts` dispatch as needed)
- Validate input using Zod
- Use clean and reusable code
- Do not duplicate logic

Files:
- booking.service.ts
- booking.schema.ts
- (HTTP wiring in `src/server/http/api-router.ts` or dedicated `app/api/.../route.ts` files)"

---

## 🔁 REFACTOR PROMPT

"Refactor this code to follow AI_RULES.md.
- Remove duplication
- Improve structure
- Move logic to service layer"

---

## ⚡ DEBUG PROMPT

"Fix this error following AI_RULES.md.
Ensure code is clean and production-ready."

---

## 🚨 ENFORCEMENT PROMPT

"If any rule is violated, fix it before continuing."